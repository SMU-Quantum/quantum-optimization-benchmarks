# Examples

This directory contains small reference files used by the research pipeline.

## Files

- `ibm_credentials.example.json`: template for rotating IBM credentials.
- `market_share_demands.json`: sample market-share demand input.

Keep real credentials out of version control. The example file documents the expected JSON shape only.
